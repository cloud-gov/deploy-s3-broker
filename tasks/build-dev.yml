---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: 18fgsa/concourse-task

inputs:
- name: broker-src-dev
- name: broker-config-dev

outputs:
- name: broker-src-built

run:
  path: broker-config-dev/tasks/build.sh
